import { defineConfig } from "cspell";

export default defineConfig({
    version: "0.2",
    languageSettings: [
        {
            "languageId": "javascript,typescript,php",
            "locale": "en, tr",
            "caseSensitive": true
        }
    ]
});